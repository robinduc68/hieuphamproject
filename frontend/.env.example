# Khi dev local (npm run dev), axios dùng proxy của Vite nên không cần set.
# Khi build production (docker), nginx tự proxy /api → backend.
# Chỉ set nếu backend chạy ở domain khác:
VITE_API_URL=/api
